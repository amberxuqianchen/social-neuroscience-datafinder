/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The site is fully static; uncomment the next line to export a static bundle
  // (e.g. for GitHub Pages). Vercel deploys work without it.
  // output: "export",
};

export default nextConfig;
